import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alvar.eldenringexplorer',
  appName: 'elden-ring-nightreign-explorer',
  webDir: 'dist'
};

export default config;
